package calendarview;

import java.time.LocalDate;
import java.util.List;

/**
 * This interface extends the original view interface, which means it has both the Text UI
 * functions and the GUI.
 */
public interface IView2 extends IView {

}
